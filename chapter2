git init
git status

How is the staging area different from the working directory and the repository? What value do you think it offers?

staging area is a subset of working directory. repository contains commits of the files in the staging area.


How can you use the staging area to make sure you have one commit per logical change?
By keeping only one file in staging area at once.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
we can go back to original state if needed. a safe way to make changes. can be branched. multiple people can work.


How do the diagrams help you visualize the branch structure?
shows connections. dependence.`

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
commits get merged and conflicts are put forth. so to understand dependency(parent) of each commit.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
pros: automatic merging merges obvious changes without conflict. show conflicts if any.
cons:just goes by line-by-line logic can be hampered with auto merging
